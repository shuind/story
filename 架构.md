# 世界与剧情创造架构

## 1. 目标

只处理世界与剧情的创造。

不直接让 AI 生成完整世界或完整剧情。

把创造继续拆向更原子的思考层，使人和多个 AI 能在足够小的层级共同探索。

世界与剧情不是一次生成出来的，而是在持续探索中逐渐显现。

---

## 2. 核心结构

整个系统由四部分构成：

```text
Plugins
   ↓
Dynamic Dimension Space
   ↓
State
   ↓
Exploration
```

其中：

* **Plugin**：一个可插拔的思考维度
* **Dimension Space**：当前所有维度及其组合形成的可探索空间
* **State**：当前已经确认并存在的世界、剧情内容
* **Exploration**：人和多个 AI 在这个空间中的持续探索

实际走过的轨迹形成 **Path**。

Path 是探索留下的结果，不是预先存在的流程。

---

# 3. Plugin

## 3.1 Plugin 是一个维度

一个 Plugin 提供一种观察、投影、连接或继续创造当前对象的维度。

```text
Object
   │
 Plugin
   ▼
Result
```

Plugin 可以作用于：

* 样本
* 元素
* 当前 State
* 多个元素
* State 与元素的组合
* 其他中间结果

---

## 3.2 Plugin 是可插拔的

Plugin 可以：

* 加载
* 卸载
* 新增
* 修改
* 替换
* 组合

系统当前拥有的 Plugin 决定当前能够进入哪些思考维度。

Plugin 不构成固定顺序。

---

## 3.3 Plugin 可以在创造过程中产生

已有 Plugin 不构成探索空间的边界。

探索过程中，如果当前内容出现了现有维度无法处理的东西，可以创造新的 Plugin。

```text
Current State
      │
发现新的观察维度
      ▼
   New Plugin
      │
      ├──重新作用于已有 State
      ├──重新作用于已有 Elements
      └──重新投影已有 Samples
```

因此，探索不仅在已有空间中进行。

**探索本身也可以扩张空间。**

---

# 4. Dimension Space

所有 Plugin 共同形成当前的维度空间。

但维度不是只能一个接一个使用。

它们可以产生多种关系。

### 串行

```text
P1 → P2 → P3
```

### 并行

```text
        P1
       /
Object ─ P2
       \
        P3
```

### 交叉

```text
P1 × P2
```

从两个维度的交汇处继续思考。

### 组合

```text
(P1 × P2) → P3
```

### 分叉

```text
       → P2
P1 ───
       → P3
```

### 汇合

```text
Branch A ─┐
          ├→ P7
Branch B ─┘
```

因此，Plugin 的意义不是组成一条固定链。

它们共同构成一个可以被不断探索、组合和扩张的空间。

---

# 5. Sample

Sample 是已有作品或其他可以吸收的对象。

例如：

* 网文
* 小说
* 科幻作品
* 神话
* 历史
* 现实材料
* 其他具有价值的对象

Sample 本身不成为一种路径。

一个 Sample 内部可以在大量不同维度下产生不同内容。

---

# 6. Element

Element 是 Sample 在某个 Plugin 维度下的投影结果。

```text
Sample × Plugin → Element
```

例如：

```text
Sample A × P1 → E1
Sample A × P2 → E2
Sample A × P8 → E3
```

同一个 Plugin 也可以投影不同 Sample：

```text
Sample A × P4 → E10
Sample B × P4 → E11
Sample C × P4 → E12
```

因此每个 Plugin 会自然积累一组属于这个维度的 Elements。

```text
Plugin P4
├── E10
├── E11
├── E12
├── E13
└── ...
```

---

# 7. Plugin 与 Elements

一个完整 Plugin 包含：

```text
Plugin
├── Dimension
└── Elements
```

### Dimension

这个 Plugin 所代表的思考维度。

### Elements

这个维度已经从不同 Sample 中得到的内容。

因此 Plugin 既提供：

**怎么看**

也携带：

**从这个维度已经看见过什么**

---

# 8. State

State 是当前创造中已经确认存在的内容。

它不是一篇不断被重新总结的“当前世界观”。

State 应保存已经出现的具体内容及其关系。

例如：

```text
State
├── A
├── B
├── C
├── A ↔ B
├── B → D
├── E 尚未闭合
└── ...
```

每一次探索主要改变 State 的局部。

```text
State
   │
一次探索
   ▼
State'
```

而不是每走一步，都让模型重新生成整个 State。

---

# 9. 一次探索

一次探索从当前 State 开始。

```text
Current State
      │
      ▼
选择 / 组合 / 创建 Plugin
      │
      ▼
取相关 Elements
      │
      ▼
多个 AI + 人
      │
      ▼
产生不同思考
      │
      ▼
修改 / 连接 / 组合 / 延伸
      │
      ▼
人确认
      │
      ▼
State 局部变化
```

然后重新面对新的 State。

继续探索。

---

# 10. 人与多个 AI

人始终处于探索内部。

多个 AI 不是分别生成完整世界。

而是在当前维度、当前局部问题上同时参与思考。

```text
               AI A
              /
State + P ─── AI B
              \
               AI C

          + Human
              │
              ▼
        Confirmed Result
```

人可以：

* 选择维度
* 组合维度
* 创建维度
* 引入元素
* 修改 AI 的结果
* 拼接不同结果
* 提出自己的内容
* 改变当前方向
* 确认 State 的变化

---

# 11. Path

Path 是探索实际发生后留下的轨迹。

例如：

```text
S0
 │
 P3
 ▼
S1
 │
P7 × P9
 ▼
S2
 ├──── P4 ───→ S3
 │
 └──── P12 ──→ S4
                │
               P21
                ▼
               S5
```

Path 可以：

* 分叉
* 回退
* 延伸
* 汇合
* 更换维度
* 在中途产生新 Plugin

因此 Path 不是系统预先提供的一条线。

它是人在动态维度空间中探索以后形成的轨迹。

---

# 12. Sample 与 Creation 使用同一套 Plugin

Plugin 不只用于分析 Sample。

同一个维度也可以直接进入创造。

### 投影 Sample

```text
Sample × Plugin → Element
```

### 作用于创造

```text
State × Plugin → State'
```

### 引入已有 Element

```text
State + Elements × Plugin → State'
```

因此，吸收和创造通过 Plugin 连在一起。

---

# 13. 整体循环

## 吸收

```text
Sample
  │
Plugin
  ▼
Element
  │
进入 Plugin 的元素集合
```

## 创造

```text
State
  │
选择 / 组合 / 创建 Plugin
  │
调用对应 Elements
  │
人 + 多 AI 思考
  │
人确认
  ▼
State'
```

## 扩张

```text
探索中发现新的维度
        │
        ▼
     New Plugin
        │
        ├──投影旧 Samples
        ├──重新观察旧 Elements
        └──继续作用于当前 State
```

三者持续循环。

---

# 14. 运行时

运行时只提供基础能力：

```text
Runtime
├── Plugin Loader
├── Model Adapters
├── Multi-model Execution
├── State Storage
├── Sample / Element Storage
├── Human Interaction
└── Revision / Branch Storage
```

运行时不理解具体世界应该怎样创造。

具体的思考能力由 Plugin 提供。

---

# 15. 最小结构

```text
Story Harness
│
├── Runtime
│
├── Plugins
│   ├── P1
│   │   ├── Dimension
│   │   └── Elements
│   ├── P2
│   │   ├── Dimension
│   │   └── Elements
│   └── ...
│
├── Samples
│
└── Projects
    └── State / Revisions
```

---

# 16. 核心关系

```text
Plugin = 可插拔思考维度

Plugin + 其 Elements = 一个可复用的创作能力单元

Plugins = 动态维度空间

Sample × Plugin = Element

State × Plugin = State 的一次局部推进

State + Elements × Plugin = 带已有积累的一次推进

探索过程中可以产生新的 Plugin

Path = 人在动态维度空间中实际探索留下的轨迹

世界与剧情 = State 在持续探索中逐渐形成的整体
```
